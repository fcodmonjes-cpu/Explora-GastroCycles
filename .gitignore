# Secrets y archivos sensibles — nunca commitear
firebase-rules.json
*.key.json
*-key.json
.env
.env.*

# Archivos de trabajo locales
*.seed.json
*.xlsx
*.pdf
*.m4a
*.png
*.svg
!Explora.png
!favicon.svg
# Reference images attached to checklist tasks (montaje, etc.) live in assets/
# and are versioned regardless of extension.
!assets/**
